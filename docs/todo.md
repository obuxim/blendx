# blendx todo

One item ≈ one focused session. Work top to bottom (see "Todo loop" in `CLAUDE.md`). Tick an item only when `bun run check` passes **and** its done-criterion is verified. One commit per item: `P5.3: <summary>`.

## P0 Scaffold
- [x] P0.1 git init, .gitignore, .editorconfig, root package.json (workspaces packages/*, examples/*), bunfig.toml (exact pins). Done: `bun install` succeeds.
- [x] P0.2 Package skeletons: blendx, spec, core, dbml, hono, cli, conformance. Done: each code package is importable by name from a test.
- [x] P0.3 tsconfig.base (TS 7, strict, bundler, verbatimModuleSyntax, noUncheckedIndexedAccess) + root tsconfig (types: ["bun"]) + tsconfig.portable.json (types: [], core + dbml src). Done: both `tsc --noEmit` runs pass, and a Bun global in core fails the portable check.
- [x] P0.4 biome.json: ignores generated paths; noRestrictedImports for bun/hono in core + dbml. Done: `biome check .` passes, and a restricted import is flagged.
- [x] P0.5 Scripts `check` / `fix` / `test` + first sample test. Done: `bun run check` passes.
- [x] P0.6 GitHub Actions workflow (check job + postgres:18 service job). Done: the workflow file exists and the same steps pass locally (no remote yet).
- [x] P0.7 CLAUDE.md, docs/todo.md, docs/decisions.md. Done: committed.

## P1 Spikes (kept as permanent regression tests)
- [x] P1.1 Explicitly typed `[validator, handler]` tuple + chained routes → `hc` types: json input, 201 body, 422 union. Done: expectTypeOf + @ts-expect-error pass under tsc, and a runtime call through `hc` works.
- [x] P1.2 `c.json(problem, 422, { 'Content-Type': 'application/problem+json' })` keeps its TypedResponse and the header. Done: runtime + type test.
- [x] P1.3 drizzle-zod 0.8.3 + zod 4.6.2 on a pgTable (varchar(n), pgEnum, identity, string-mode timestamp, double). Done: a max-length error test and a z.infer type test pass, and the schemas unify with the `zod` import.
- [x] P1.4 @dbml/core parses the addition DBML + a kitchen-sink DBML. Done: a snapshot of fields/refs/enums/indexes is committed.
- [x] P1.5 PGlite + drizzle-orm/pglite migrator applies a drizzle-kit-generated migration inside bun test. Done: test passes.
- [x] P1.6 `z.toJSONSchema` on drizzle-zod schemas (io input/output, draft 2020-12). Done: snapshot committed, no unrepresentable-type errors.
- [x] P1.7 @typescript/typescript6 extracts `calculate` source + return-type keys (literal, spread, conditional). Done: test returns the expected keys.
- [x] P1.8 Confirm or amend ADRs D5 to D8 with spike results. Done: decisions.md updated.

## P2 DBML → IR → Drizzle
- [x] P2.1 SchemaIR types + conventions (timestamps, soft delete via `deleted_at`, generated columns). Done: types + IR unit test.
- [x] P2.2 `parseDbml` → IR, errors with line/col. Done: addition, kitchen-sink and syntax-error tests.
- [x] P2.3 Type-mapping table (int/serial/bigint/varchar/char/text/bool/double/real/numeric/date/timestamp[tz]/time/uuid/json[b]/enum/arrays; unknown → error). Done: table-driven tests.
- [x] P2.4 IR validation (missing PK, composite PK, reserved query-name columns, identifier collisions). Done: tests.
- [x] P2.5 `emitDrizzle` → schema.gen.ts (pgEnum, pgTable, identity, references + onDelete, named uniques/indexes, `models` const + meta). Done: golden test.
- [x] P2.6 Generated schema passes tsc; drizzle-kit generates SQL; PGlite migrates. Done: integration test.
- [x] P2.7 `meta.constraints` (constraint name → kind, columns). Done: golden + test.

## P3 Authoring API & types (highest risk: type inference)
- [x] P3.0 Inference spike: choose the blend() authoring shape. Done: one call per action (`actions: (a) => [a.index(), a.store({ rules, calculate })]`) infers in every case; recorded as D12.
- [x] P3.1 Model / Row / Insert / Writes / Column / PublicRow helpers. Done: type tests against the P2 golden.
- [x] P3.2 Policies `allow.public | authenticated | owner(col) | when(fn)`, `deny`, `requiresAuth`. Done: unit + type tests.
- [x] P3.3 `blend()` + ResourceSpec with the D12 action builder (required policy, the actions array as the exposure list, `a.restore()` only with soft delete, real hidden columns, custom actions via `a.member` / `a.collection`). Done: positive + @ts-expect-error tests.
- [x] P3.4 Hook signatures: rules → calculate input inference (`() => z.object()` and `({prev}) => prev.extend()`), no db/request in calculate, unknown write key is an error, respond keeps its status literal. Done: type tests.
- [x] P3.5 `defineApp` (type-preserving app hooks, auth) + `Register` module augmentation. Done: type tests with a hand-written register.
- [x] P3.6 EndpointDefinition + `toEndpoints(resource)`, deterministic order. Done: unit test.
- [x] P3.7 `defineConfig` + config loader/validation. Done: tests.

## P4 Rules & cascade
- [x] P4.1 Default rules per action (store: strict insert minus generated; update: partial; index: query; member: none; custom: empty). Done: one test per derivation rule id.
- [x] P4.2 `packages/spec/derivation-rules.md` table mirroring the test ids. Done: every rule id links to a test.
- [x] P4.3 Cascade resolver schema → app → resource → action, `prev` vs `runDefault`, provenance recorded. Done: replace/extend tests at every level.
- [x] P4.4 Policy → authorize hook, schema-level deny. Done: tests.

## P5 Engine
- [x] P5.1 ProblemDetails model + builders (400/401/403/404/409/422/500) + zod issues → JSON pointers. Done: unit tests.
- [x] P5.2 `execute()` skeleton + early 401 + walking skeleton: addition store on PGlite. Done: 201 with the saved record (`result: 7`).
- [x] P5.3 Default load: findOrFail, soft-delete scope, restore loads trashed rows, index list with pagination/sort/filter. Done: tests.
- [x] P5.4 Default save: insert/patch/delete/soft delete/restore, timestamps, returning, writable-key guard. Done: tests.
- [x] P5.5 Default respond: 201/200/204, index envelope, hidden fields stripped. Done: tests.
- [x] P5.6 Transactions + `FOR UPDATE` on member mutations; respond after commit. Done: rollback test (lock test on real PG).
- [x] P5.7 PG error mapping: 23505 → 409, 23503/23502/22P02/22001 → 422 with pointers. Done: tests.
- [x] P5.8 Custom member/collection action defaults. Done: tests.

## P6 Hono adapter
- [x] P6.1 BlendxEnv + `createServer` (db/auth injection, onError/notFound → problem, malformed JSON → 400). Done: app.request tests.
- [x] P6.2 `run()` with explicit tuple types delegating to `execute`. Done: all addition actions work over hand-written routes.
- [x] P6.3 RPC type tests (per-action input, status narrowing, problem type, 204). Done: tsc.
- [x] P6.4 Node smoke test: @hono/node-server + pg driver. Done: script exits 0.

## P7 Generators & CLI
- [x] P7.1 routes.gen.ts emitter (thin, sorted, collection customs before `/:id`, exports AppType). Done: golden + tsc.
- [x] P7.2 register.gen.ts + drizzle.config.gen.ts emitters. Done: goldens.
- [x] P7.3 CLI shell (util.parseArgs, help, exit codes). Done: `blendx --help` works.
- [x] P7.4 `blendx generate`, two-phase (DBML → schema; then blends → routes/register/openapi). Done: fixture output equals goldens.
- [x] P7.5 `generate --check` with unified diff. Done: drift exits 1.
- [x] P7.7 `createDatabase(config)` with dynamic driver imports (pg, postgres-js, bun-sql, pglite). Done: pglite + pg tests. (Before P7.6: `migrate up` opens the database through it.)
- [x] P7.6 `blendx migrate generate` (drizzle-kit subprocess) + `migrate up` (per-driver migrator). Done: applies on PGlite + local PG.

## P8 examples/addition
- [x] P8.1 schema.dbml, blendx.config.ts, src/app.ts, blends/addition_results.ts, server.ts; generated artifacts committed. Done: `generate --check` passes.
- [x] P8.2 E2E on PGlite: `{a:4,b:3}` → 201 `result: 7`; 422 bad type; 422 unknown key; 404 show; soft delete hides from index; restore. Done: bun test.
- [x] P8.3 `hc<AppType>` typed + runtime client test. Done: tsc + test.
- [x] P8.4 Same suite on local PostgreSQL 18 (`BLENDX_TEST_DB=pg`). Done: passes.

## P9 OpenAPI
- [x] P9.1 OpenAPI 3.1 builder (paths, params, request bodies io:input, responses per status, Problem component, public-record components, sorted keys). Done: snapshot.
- [x] P9.2 Validate the output with @readme/openapi-parser. Done: test.
- [x] P9.3 Wire openapi.json into generate / --check; replies it cannot describe print warnings. Done: golden.
- [x] P9.4 `reply` on an action (D14): a type-checked schema for a reply blendx can't derive, so OpenAPI describes it instead of warning. Done: type tests + OpenAPI snapshot.

## P10 Review
- [x] P10.1 Review model: resolved stages + provenance + compact rules from JSON Schema. Done: unit test.
- [x] P10.2 `calculate` source + `writes` via the typescript6 program (default calculate → rules keys). Done: literal/spread/conditional tests.
- [x] P10.3 YAML emitter (yaml Document API, `# from:` comments, `format: 1`, deterministic). Done: golden `review/addition_results.yaml`.
- [x] P10.4 `blendx review` + `--check` (diff, exit codes). Done: drift test.
- [x] P10.5 Examples runner (validate → calculate → deep-equal), usable from bun test and inside `--check`. Done: pass case + readable failure.
- [x] P10.6 Fix-request workflow test (edit YAML → check fails → change blend → passes) + doc. Done: test + doc.

## P11 Conformance
- [x] P11.1 Case format + matchers ($any, $int, $timestamp, $absent; `$timestamp` replaces the planned `$iso8601`, see D15) in `packages/spec/conformance.md`. Done: doc + types.
- [x] P11.2 `runConformance(cases, fetch, { reset })`. Done: unit tests.
- [x] P11.3 Shop fixture (users: hidden password, unique email; orders: FK, enum, soft delete, owner policy, `refund` member action, `quote` collection action, filters). Done: `generate --check` passes.
- [x] P11.4a Harness: serve the shop fixture in-process (committed migration, reset = truncate + `seed.sql`), load `cases/*.json`, raw `text` request bodies; one case per error status (400, 401, 403, 404, 409, 422). Done: the suite passes on Bun + PGlite.
- [x] P11.4b One case per derivation rule. Done: coverage meta-test (every DR id and every error status has a case) passes.
- [x] P11.5 Matrix: Bun + PGlite, Bun + pg, Node + pg. Done: all three pass (locally, and as CI jobs).
- [x] P11.7 `databaseError` also reads the SQLSTATE from `errno` when `code` is not one, as bun-sql's `PostgresError` has it (D16); bun-sql joins the conformance matrix. Done: all cases pass on bun-sql, and a unit test without a database.
- [x] P11.6 bun-sql opt-in run, results recorded. Done: ADR entry.

## P12 Docs & polish
- [x] P12.1 Spec docs: pipeline, cascade, errors, review-format (each linked to test ids). Done: docs exist, links resolve.
- [x] P12.2 Final CLAUDE.md + app-author CLAUDE.md template in examples/addition. Done: dry-run "add a table + a custom action" using only the docs.
- [x] P12.3 One-page blend cookbook (10 patterns). Done: doc.
- [x] P12.4 `bun build --compile` CLI. Decided against for v1 (D17): the CLI ships only as a package bin, `bunx blendx` (`@blendx/cli`'s since D18).
- [x] P12.5a Split the runtime from the CLI (D18): `blendx` drops its bin and its dependency on `@blendx/cli`, which owns the `blendx` bin and `@blendx/cli/examples`. Done: `bunx blendx` works from the repo and from the example, and `bun run check` passes.
- [x] P12.5b README. Done: README.
- [x] P12.6 Drizzle 1.0 GA migration spike (drizzle-orm/zod, kit folder format). Done: ADR go/no-go. Decided in D19: no-go until 1.0.0 is on npm `latest`; the upgrade checklist is in D19.

## P13 Own our dependencies' gaps (don't wait on upstream)
- [x] P13.1 Upgrade to Drizzle 1.0.0-rc.4 now, exactly pinned (D20 replaces D19's wait): `drizzle-orm/zod` instead of drizzle-zod, the type renames, migration folders converted with `drizzle-kit up`, `blendx migrate` on the 1.0 layout. Done: `bun run check`, plus the pg, bun-sql and Node rows of the matrix, pass.
- [x] P13.2 Our own DBML parser for the subset blendx accepts, replacing @dbml/core and the oven-sh/bun#42512 workaround (D21). Done: the DBML tests and goldens pass on it, @dbml/core and `loadDbmlCore()` are gone.
- [x] P13.3 An index on an expression marked `pk`, `` (`lower(id)`) [pk] ``, is refused with the same located error as any other expression index. Today it gives `primaryKey: ['lower(id)']`. (Found by the P13.2 review; the bug predates P13.2.) Done: the parser test refuses both kinds of expression index, each at its line.
- [x] P13.4 Composite refs that are the same relation written differently, `a.(x, y) > b.(p, q)` and `b.(q, p) < a.(y, x)`, are reported as defined twice. (P13.2 review.) Done: the parser test reports that pair, and not a ref that pairs the same columns differently.
- [x] P13.5 A `'''` string takes the escapes upstream DBML gives it, so it can end in a quote. Today the first `'''` closes the string, and `'''it is ''a''''` is reported as unterminated. (P13.2 review.) Done: `'''` strings take the escapes of `'...'` strings, and a `\` at the end of a line joins the next, as upstream's lexer does (`@dbml/parse`, `escapedString`).
- [x] P13.6 Strings take the rest of upstream's escapes: `\r`, `\0`, `\b`, `\v`, `\f` and `\uHHHH`, and `\ ` keeps its backslash. Today blendx reads each as the bare character (`\r` is `r`), in `'...'` and `'''...'''` strings alike. (Found while doing P13.5.) Done: the parser test reads each escape as upstream does, and refuses a `\u` without four hex digits, at its backslash.
- [x] P13.7 A `'''` string's escapes and its indentation removal run in the order upstream uses. Today blendx applies escapes first, so a `\n` escape starts a line that is dedented too. (Found while doing P13.5.) Done: upstream's `normalizeNote` (`@dbml/parse`, `core/utils/interpret.ts`) gets the lexer's value, escapes already read, so the order was already upstream's; a parser test pins it.
- [x] P13.8 Notes are trimmed as upstream trims them (`normalizeNote`, `@dbml/parse`'s `core/utils/interpret.ts`): leading blank lines dropped, then the common indentation, in either kind of string, and a trailing line break kept. Other strings are kept as written. Today blendx removes the first and last line break of every `'''` string, and its indentation. (Found while doing P13.7.) Done: `normalizeNote` is ported, and a parser test pins a note in each kind of string, one with CRLF, and a `'''` default kept as written.

## P14 A guide for app authors, and a second example
- [x] P14.1 `examples/expenses`, built from scratch as the guide will tell it: users who sign up for a bearer token, expense claims with an owner policy and an approver role, state rules on member actions, pure pricing in calculate, a `quote` collection action, review examples and end-to-end tests. Done: `bun run check` passes with the example in its typecheck, generate and review checks.
- [x] P14.2 `docs/guide/`, the app author's guide: getting started (in the monorepo, or in an app of its own through `bun link`), a tutorial that builds `examples/expenses`, and reference pages (schema, blends, hooks, the app and identity, the HTTP API and typed client, review, testing, configuration and deployment, the CLI). README and CLAUDE.md link to it. Done: every command and reply the guide shows was run, and every link resolves.
- [x] P14.3 `docs/guide/known-issues.md`: the Inbox findings of P14.1 and P14.2 that app authors meet, each with what happens and what to do instead, linked from the guide index, the README and the pages that mention them. An entry leaves the page when its Inbox item is fixed. Done: every link resolves, and each entry matches an Inbox item.

- [x] P14.4 A narrated, interactive walkthrough of both examples in `docs/media/walkthrough/`: `script.ts` holds the steps and their narration, `build.ts` captures every command and reply from the running examples and voices the narration with Piper (the public-domain LJSpeech voice), and `index.html` plays it step by step. Done: every output on the page comes from a build run, the page lints clean, and the guide links to it.

- [x] P14.5 The guide, the cookbook and the walkthrough as one site: `docs/site/build.ts` renders the Markdown with Bun's own renderer into `site/` (not committed), links pages to each other and repository files to GitHub, fails on a link that resolves to nothing, and copies the walkthrough to `/tutorial/`; `vercel.json` tells Vercel how to build and serve it. Done: the site builds with every link resolved, and `bun run check` passes.
- [x] P14.6 Serve the site from Vercel at `blendx.zubairhasan.com`: a Vercel project linked to the repository, redeploying when `docs/` changes, with the domain attached. Done: `/docs/` and `/tutorial/` load from the domain. (Needs the owner's go-ahead, and a DNS record for the subdomain.)

## P15 Fixes from the guide
Found while building `examples/expenses` and writing `docs/guide` (P14). Each item also removes its entry from `docs/guide/known-issues.md`, and updates the guide and the example where they work around it.
- [x] P15.1 App hooks type `auth` from the app's own `auth` function, not through `Register`: going through `Register` makes an app-level authorize hook that reads `auth` a circular type (TS2502). Done: a type test in the Register project, with an app authorize hook that reads `auth`, passes tsc; the guide shows app hooks reading the identity.
- [x] P15.2 SQLSTATE 22007 (invalid datetime format) and 22008 (datetime field overflow) answer 422 with a pointer, instead of 500 (D23). Done: an engine test, `packages/spec/errors.md`, and a conformance case.
- [x] P15.3 Date and timestamp columns check their format by default (D23): a date is `YYYY-MM-DD` naming a real day; a timestamp is ISO 8601 or PostgreSQL's text form. Done: a derivation rule id with its test, spec entry and conformance case; OpenAPI and the review describe the forms; `examples/expenses` drops its own `spent_on` rule.
- [x] P15.4 calculate's `prev` is typed as the writable columns the input can carry, not all of `Writes<M>`, so the review of `({ prev }) => ({ ...prev, total })` names exactly those columns. Done: type tests, and a review test with a spread `prev`.
- [x] P15.5 The review describes a string with a format by the format alone, without zod's pattern. Done: a json-schema test; the review goldens and the example review files updated.
- [x] P15.6 An action whose rules are the empty object takes no body in its route type, as OpenAPI already gives it no request body. Done: an RPC type test; the expenses client test calls `submit` without `json: {}`.
- [x] P15.7 index takes a declared `scope` (D22): column equalities the default load adds to its filters, failing closed on a missing value, with its source in the review. Done: tests that pages and totals count only the rows in scope, a spec entry and a conformance case, cookbook pattern 7 rewritten, and `examples/expenses` lists a claimant's own claims without `?user_id=`.
- [x] P15.8 One action's reply may carry a hidden column, such as a token returned once at sign-up. Decide the shape first (D24; proposed during triage: a `reveal` list on the action). Done: D24; type tests; the engine, OpenAPI and the review show which replies carry the column; `examples/expenses` hides `api_token` except in the sign-up reply.

## Inbox
Discovered work goes here. Triage it into a phase before starting it.
- [x] Remove the postMessage workaround in `packages/dbml/src/dbml-core.ts` once `packages/dbml/test/bun-42512.test.ts` fails (Bun fixed oven-sh/bun#42512). Removed by D21 without waiting: blendx parses DBML itself.
- [x] P4.1: decide whether to drop drizzle-zod's ±2^47 bounds on `doublePrecision` columns (real doubles like 1e20 are rejected today). Decided in D13: dropped.
- [x] P9.1: add `format: date-time` / `date` to string-mode timestamp and date columns in OpenAPI output. Decided in the D8 P9.1 note: `format: date` on date columns only; Postgres timestamps are not RFC 3339, so they stay plain strings.
- [x] P12.5: the `blendx` facade depends on `@blendx/cli` for its bin (D9 note), so production installs also get the CLI's dependencies (drizzle-kit, typescript6 once review lands). Decide before publishing whether to make them optional or lazy. Decided in D18: split; now P12.5a.
- [x] Upgrade to Drizzle 1.0.0 once it is on npm `latest`, following the D19 checklist, with the new pins recorded (D5). Now P13.1, without waiting.
- [x] Date and timestamp columns accept any string (DR-DATE-STRING), and PostgreSQL decides. It reads words such as `yesterday` as dates, and a value it cannot read, such as `2026-02-30`, answers 500 (SQLSTATE 22008, or 22007 when malformed) instead of 422. Decide between a format in the derived rule, mapping 22007 and 22008 to 422, or both. (Found while building P14.1, whose `spent_on` rule is `z.iso.date()` for now.) Decided in D23, both: now P15.2 and P15.3.
- [x] The review lists every writable column under `writes` for a calculate that spreads `prev` (`({ prev }) => ({ ...prev, total })`): `prev` is typed as all of `Writes<M>`, though at run time it holds only the input's writable columns. Type `prev` from the input so the review names what is written. (P14.1 spreads `input` instead.) Now P15.4.
- [x] The review prints zod's whole regex after a string format: `spent_on: string (date), matching ^(?:(?:\d\d[2468]...`, and the same for `email` and `uuid`. The format alone says it. (P14.1.) Now P15.5.
- [x] An index scoped to the requester has no correct pages: a load hook's `runDefault()` takes no filter, and cookbook pattern 7 filters one page after loading it, so `meta.total` and the page sizes are wrong once rows span pages. Consider `runDefault({ where })` or a scope hook. (P14.1 asks non-approvers for `?user_id=` and checks it in authorize.) Decided in D22, a declared `scope`: now P15.7.
- [x] A column that one reply must carry and every other reply must hide, such as an API token returned once at signup, has no way to say so: `hidden` applies to the whole resource, and respond sees only the public record. (P14.1 leaves `api_token` visible and exposes users only through store and an owner-only show.) Now P15.8, which decides its shape first.
- [x] An app-level authorize hook that reads `auth` does not typecheck in a registered app: `RegisteredAuth` comes from `typeof app`, whose hook types need `RegisteredAuth`, so tsc reports TS2502 (`'auth' is referenced directly or indirectly in its own type annotation`), even with the hook's parameter annotated. It runs correctly, and resource hooks and policies read `auth` with its type. (Found while writing the P14.2 guide, which points app authors to resource hooks for now.) Now P15.1.
- [x] The typed client makes a custom POST action without rules take `json: {}` (`client.expenses[':id'].submit.$post({ param, json: {} })`), while built-in `restore` takes only `param`. Decide whether an action whose rules are the empty object should take no body in its route type. (P14.2's client test passes `json: {}`.) Decided: it takes none, as OpenAPI already says; now P15.6.
- [x] A guide page for `@blendx/react` in `docs/guide/`, once N.2 and N.3 settle invalidation and form errors: creating the client, queries and mutations, keys, errors. (Found in N.1b.) Now N.6.
- [x] Queries ignore TanStack's abort signal: the queryFn calls hc without the query's `signal`, so a cancelled query's request still runs to the end. Check how hc merges a call's `init` with the client's own before passing it. (Found in N.1b.) Now N.7.
- [x] `@blendx/react` misses includes when it invalidates: a mutation invalidates its own table's queries (N.2, written when replies held only their own rows), but since D28 a query with `?include=` holds rows of another table, so `orders.index.queryOptions({ query: { include: 'user' } })` keeps the old user after `users.update`. Proposed: `client.gen.ts` records which tables include which, and a write also invalidates the queries of the tables that include its own. Fix before optimistic updates, which would share the gap. (Found while discussing more for the React adapter; after P16.10.) Now N.8.
- [x] Infinite scroll on index in `@blendx/react`: `index.infiniteQueryOptions(input)`, the next page derived from `meta` (`page + 1` while `page * per_page < total`), with `page` left out of its input. Its key must differ from the plain query's, since TanStack says not to share one between them, but keeps the `[table]` prefix so mutations invalidate it. The guide also shows numbered pages with `placeholderData: keepPreviousData`, which works today. Offset paging repeats a row in a newest-first list when one is inserted between pages, and skips one when a row is removed; cursor paging would fix that, but it changes the server's contract, so it would be a P16 candidate of its own. (Found while discussing more for the React adapter; after P16.10.) Now N.9.
- [x] Optimistic updates in `@blendx/react`, decided first (D30), store included. Proposed while discussing: opt-in per mutation; update merges its `json` into every cached copy of the row (its show, and the index pages holding it), destroy removes them, a custom member action takes `optimistic: (row, input) => row`, and store inserts a row with a temporary id. To decide for store: which cached lists get the row (their filters, sort, scope and page), and what it holds for what the server fills (id, defaults, calculate, timestamps, hidden columns). Also proposed: it runs inside the mutation function, as invalidation does, so an app still spreads its own options; a failure invalidates instead of restoring a snapshot, which a retry or a second mutation would make wrong; `client.gen.ts` records each table's primary key column, which need not be `id`. (Found while discussing more for the React adapter; after P16.10.) Now N.10 to N.12.

## N React adapter
Started 2026-09-13 on D25: a client calls an action by its table and name, and gets TanStack Query options, not hooks.
- [x] N.1a `blendx generate` writes `src/generated/client.gen.ts`: every action's `METHOD /path` by table and action name, importing nothing (D25). Done: a golden from the shop blends, a type test that its entries are exactly the routes of the same blends' AppType, and the examples and fixtures regenerated.
- [x] N.1b `@blendx/react`: `createBlendxClient(hc<AppType>(url, options), endpoints)` (D25 note). A GET action gives `queryOptions(input)`, any other method `mutationOptions()`, keyed `[table, action, input]` and `[table, action]`; each resolves to the body of the action's success reply (`null` for a 204) and rejects with a `ProblemDetailsError` otherwise. The react and @tanstack/react-query pins are in decisions. Done: type tests that an action's input and data are `hc`'s and that the options fit `useQuery`, `useSuspenseQuery`, `useMutation` and `fetchQuery`; runtime tests through a QueryClient against the conformance shop fixture.
- [x] N.2 Invalidation (D25 note): a mutation that succeeds invalidates every query of its table, and of the tables `mutationOptions({ invalidates })` names, and resolves once the active ones have refetched. It takes the QueryClient from TanStack's mutation context, so `createBlendxClient` needs none. Done: runtime tests (active queries refetch before the mutation resolves, other tables only when named, a failure invalidates nothing, a spread `onSuccess` keeps it) and a type test that `invalidates` takes only the map's tables.
- [x] N.3 Form field errors (D25 note): every action's `fieldErrors(error)` gives the first problem with each field of a refused input, keyed by the input's typed fields (a body field by its path, such as `tags.0`; a query parameter by its name), and `{}` for any other error. Done: runtime tests for a refused body, a refused query and a 409 on a unique column; tests for pointer escapes and for errors that are not refusals; a type test that the keys are the input's fields.
- [x] N.4 Type tests: input/output parity with `hc` for every kind of action. Done: `packages/react/test/types/kinds.types.test.ts` lists each kind once in an inline app (the six built-in actions; custom member actions by POST with and without rules, GET, PATCH and DELETE; custom collection actions by GET, POST with rules and POST without), checks its map against `toEndpoints`, and checks that each action's input and data are hc's for its route.
- [x] N.5 `examples/addition/web` (D25 note): a React and Vite page over the addition API through `@blendx/react`: the store's mutation, the index query refreshed by invalidation, and refused fields from `fieldErrors`. Done: Playwright tests in Chromium (`bun run e2e:web`, and CI's `e2e-web` job) against the app's routes on in-memory PGlite; the page typechecks as its own project in `bun run check`.
- [x] N.6 A guide page for `@blendx/react` in `docs/guide/`: creating the client, queries and mutations, keys and invalidation, errors and `fieldErrors`, walking through `examples/addition/web`. Done: `docs/guide/react.md`, linked from the guide index, the HTTP page and the site's navigation; every line of its 12 snippets is in the example or the adapter's tests, and the site builds with every link and anchor resolved. The abort-signal Inbox item is on the known issues page.
- [x] N.7 A query passes TanStack's abort signal on to its request, as the call's `init.signal`, which hc merges into the client's own `init`; a cancelled query then aborts its request. Done: a test that cancelling a query aborts the request and keeps the client's `init`; the known issue leaves the guide, and the React page says it.

## P16 Deferred features (D10)
Started 2026-09-13. D10 left these out of v1; each is decided before it is built.
- [x] P16.1 The after stage (D26): an `after` hook on actions that write, at the app, resource and action levels, run in that order once the write has committed and before respond; a failure goes to `onError` and leaves the reply. Done: engine tests (the committed row, the loaded row, the level order, failures, no after on a failed write or a read), type tests, blend() refusing it on reads, and the pipeline and cascade specs.
- [x] P16.2 after in the review and the docs: the review lists `after` on actions where a hook sets it, with its levels; the guide's hooks page, a cookbook pattern, and the CLAUDE.md templates. Done: review tests and goldens, and every doc link resolving.
- [x] P16.3 later hooks write outbox entries (D27): the `later` hook at the app, resource and action levels on actions that write; core's `blendx_outbox` table; the engine writes one entry per level in the action's transaction. Done: engine tests (an entry per level with its JSON context, a write that fails leaves none, a missing outbox table fails the write and rolls it back, reads write none), type tests, blend() refusing it on reads, and the pipeline and cascade specs.
- [x] P16.4 The outbox worker (D27): `drainOutbox()` and `startOutbox()` claim due entries with `FOR UPDATE SKIP LOCKED` and a lease, run each level's hook, delete an entry that succeeds, retry one that fails after a growing delay, keep it as failed after the last attempt, and report failures to `onError`; `routes.gen.ts` exports the app's `resources`. Done: worker tests on PGlite, and a real-PostgreSQL test that two workers never run one entry twice.
- [x] P16.5 The outbox table from the hooks (D27): `blendx generate` writes `outbox.gen.ts`, which re-exports the table once any later hook exists and nothing otherwise, and drizzle.config.gen.ts lists it. Done: goldens, and `blendx migrate generate` creating the table for a fixture with a later hook.
- [x] P16.6 later in the review and the docs: the review lists `later` where a hook sets it; the guide (hooks; deployment: running the worker), a cookbook pattern and the CLAUDE.md templates. Done: review tests, and every doc link resolving.
- [x] P16.7 Relations, and includes declared (D28): a model's belongs-to relations, derived from its foreign keys; `includes: { user: users }` on a blend, checked by the types and by `blend()`: a relation of the table, a blend of the table it points to, which exposes show without a show load hook, and no column already named like the relation. Done: type tests and blend() tests.
- [x] P16.8 `?include=` on index and show (D28): the derived rules accept `include` (a new derivation rule), and the engine loads each relation's rows in one query, passes each through the target's show authorization, removes its hidden columns and nests it, or `null`; the reply types carry the optional, nullable fields. Done: engine tests (authorization per row, soft-deleted and missing targets, hidden columns, one query per relation, unknown names answering 422), the rule's test and spec entry, reply type tests.
- [x] P16.9 Includes in OpenAPI, the review, conformance and the docs: the `include` parameter and the nested schemas, the review's includes, conformance cases on the shop fixture, the guide (blends, the HTTP API), a cookbook pattern and the CLAUDE.md templates. Done: OpenAPI and review tests, the conformance cases on every runtime (the DR-INCLUDE case came with P16.8, which the coverage test required), and every doc link resolving.
- [x] P16.11 Has-many includes declared (D31): `includes: { notes: { blend: order_notes, limit: 10 } }` names the rows of another table whose foreign key points at this one, with `by` when the target points at it more than once and `sort` for its order; `limit` is required on every has-many include, and a bare blend stays a belongs-to. The types and `blend()` check that the target's model has exactly one single-column foreign key to this table, or that `by` names one; that the name is neither a column nor a belongs-to relation of the table; that the target exposes show without a load hook; that `sort` is a column of the target and not hidden there. Done: type tests and blend() tests, the belongs-to checks unchanged; `resolveIncludes` gives every consumer one shape for both kinds, and the engine refuses to serve a has-many until P16.12.
- [x] P16.12 Has-many rows in the reply (D31): the engine loads each has-many relation's rows in one query for the whole reply, at most `limit` per parent row by a window function over the foreign key, in the declared order or by the target's primary key, live rows only; each row goes through the target's show authorization and loses its hidden columns, and a refused row is dropped; the array is nested under the name, empty when there are none; the reply types carry it as an array of the target's public record. Done: engine tests on PGlite and on real PostgreSQL (the limit per parent, the order, a refused row dropped, soft-deleted rows left out, one query per relation, a show page and an index page, an empty list), DR-INCLUDE extended in the spec and its test, reply type tests, and the pipeline spec.
- [x] P16.13 Has-many includes in OpenAPI, the review, conformance and the docs: the nested array schema; the review's includes naming the target, its limit and order; `client.gen.ts` recording the target table so the React adapter's invalidation follows it; conformance cases on the shop fixture (`orders` including `notes`); the guide (blends, the HTTP API, the React client), a cookbook pattern and the CLAUDE.md templates. Done: OpenAPI, review and client map goldens, the conformance cases on every runtime, an adapter test that a write to order_notes refetches the orders queries that include it, and every doc link resolving.
- [x] P16.10 Force-delete (D29, written first): `a.purge()`, a built-in action on soft-delete tables only, served as `DELETE /<table>/:id/purge` and answering 204 with no body. It has its own policy, like every action; it loads the row whether it is soft-deleted or not, locked `FOR UPDATE`, authorizes, and deletes it for good in the action's transaction. A row other rows still reference answers 409, as destroy does on a table without soft delete; `after` and `later` see the deleted row as `saved`. The types and `blend()` refuse it on a table without soft delete, where destroy already deletes for good. Done: engine, type and blend() tests; DR-MEMBER-EMPTY and the pipeline spec; a conformance case on the shop fixture; OpenAPI and the review; the guide and cookbook pattern 10.
- [x] P16.14 Nested includes served (D32): `?include=user.team` follows a path through the includes of the included blends, belongs-to and has-many alike, with the legal paths derived from the blends and a path asking its prefixes; DR-INCLUDE extended to dotted paths, an unknown segment answering 422; the engine nests level by level, one query per path for the whole reply, each level through its own show, a null or dropped row nesting nothing below it, a has-many's limit at each level, a hidden foreign key still nesting; the reply types carry a nested include as an optional field of the included record, recursively. Done: engine tests on PGlite and on real PostgreSQL (has-many under has-many), the rule's tests, reply type tests, DR-INCLUDE in the spec and its test, and the pipeline spec.
- [x] P16.15 Nested includes in OpenAPI, the review, the React adapter, conformance and the docs (D32): OpenAPI inlines an included record that has includes of its own and registers every path's component; the review lists one line per path; `@blendx/react` walks a query's paths through the client map, so a write to any table the query holds refetches it; the shop fixture's `order_notes` gains a nullable `author_id` and includes `author`, with a migration, seed rows and cases for `orders?include=notes.author`; the guide (blends, the HTTP API, the React client), a cookbook pattern and the CLAUDE.md templates. Done: OpenAPI and review tests, the client map golden, the conformance cases on every runtime, an adapter test, and every doc link resolving.

## N React adapter, continued
Triaged 2026-09-13 from the Inbox, to follow P16.10: more on top of `@blendx/react`.
- [x] N.8 Invalidation follows includes: `client.gen.ts` records which tables each table includes, and a mutation also invalidates the queries of the tables that include its own, those whose `include` asks for it. A D25 note settles the map's new shape. Done: goldens from the shop blends, with the examples and fixtures regenerated; runtime tests that a write to an included table refetches a query that includes it and leaves one that does not; the guide's invalidation section.
- [x] N.9 Infinite scroll on index: `index.infiniteQueryOptions(input)` on every index, its next page `meta.page + 1` while `page * per_page < total`, `page` left out of its input, and a key of its own under `[table]`, since TanStack says not to share one with the plain query. Done: runtime tests on the shop fixture (pages fetched up to `total` and none after, a mutation refetching every loaded page); type tests (no `page` in its input, the options fit `useInfiniteQuery`, `useSuspenseInfiniteQuery` and `fetchInfiniteQuery`); a guide section on infinite scroll and on numbered pages with `placeholderData: keepPreviousData`, which says that a newest-first list repeats a row inserted between pages.
- [x] N.10 Optimistic updates (D30, discussed and written first, store included, since it shapes the mechanism): opt-in per mutation, `mutationOptions({ optimistic })`. update merges its `json` into every cached copy of the row (its show and the index pages holding it, infinite ones included), destroy removes them, and a custom member action takes `optimistic: (row, input) => row`. It runs inside the mutation function, as invalidation does, so an app's spread options keep it; a failure invalidates the queries instead of restoring a snapshot (N.2's failures invalidate nothing). `client.gen.ts` records each table's primary key column. Done: D30; runtime tests (the change is in the cache before the reply, a failure brings the server's rows back, a spread `onMutate` and `onSuccess` keep it); type tests (`true` on update and destroy, a function on custom member actions).
- [x] N.11 Optimistic store (D30): the new row, with a temporary id, goes into the cached lists D30 chooses, holding what D30 says for the columns the server fills, and the server's row replaces it once the reply arrives. Done: runtime tests (the row in the chosen lists before the reply, replaced by the server's row, gone after a failure) and type tests.
- [x] N.12 Optimistic updates in the guide and the example: a guide section (opting in, what each action changes, failures, and TanStack's `mutation.variables` pattern for what the adapter cannot derive), and the addition page's store made optimistic. Done: a Playwright test that the new result shows before a delayed reply; every line of the guide's snippets in the example or the adapter's tests, and every link resolving.

## P16 Deferred features, continued
Triaged 2026-09-13 from the roadmap's remaining D10 candidates, in this order. Each is decided before it is built, and the decision splits it into build items, as D32 did for P16.14 and P16.15.
- [x] P16.16 Composite primary keys, decided first (D33): one path segment per key column, named by the column, in the key's order; `:id` stays for a single column; key columns are input; relations stay single-column. Split into P16.16a to P16.16d.
- [x] P16.16a The model and the schema (D33): `ModelMeta.primaryKey` becomes the list of key columns, `schema.gen.ts` writes every column of the key and a composite one as a `primaryKey({ name, columns })` table constraint, validation accepts a composite key (P2.4's refusal goes), the derived index filter covers each key column, and `generated` stays as it is (a composite key is never generated). Done: dbml tests and a kitchen-sink golden, a PGlite test that Postgres holds `order_items_pkey` over both columns and names it on a duplicate, DR-STORE-GENERATED and DR-INDEX-FILTER tests on the composite table, the shop fixture's `order_items` table with its migration (no blend yet), every `primaryKey` consumer in core and the CLI reading the list (save finds a row by every key column, the index and has-many tiebreaks order by all of them; a composite member load throws until P16.16b), and `bun run check` green.
- [x] P16.16b Routes and the engine (D33): `blend()` builds member paths from the model's key through `memberPath()`, `/:order_id/:line` for a composite key and `/:id` for a single one, custom member actions below them; the engine's member load and lock match every key column from the path parameters, a segment that cannot be its column's type answers 404, save finds the row by every column, the default index sort and the has-many tiebreak order by all of them. Done: `engine-composite.test.ts` on PGlite over the kitchen-sink golden (the routes, show, a 404 for a pair that names no row and for a segment of the wrong type, the index order with and without a sort, a has-many include's order, update, a custom member action, store with the key as input, a 409 on the key, destroy), an hc type test that the route takes one param per column and the key columns in the body, and the pipeline and errors specs.
- [x] P16.16c OpenAPI, the review, the client map and the React adapter (D33): one path parameter per key column with its column's schema; the review's route lines, and a member action told from a collection one by any key segment; `client.gen.ts` records `key` as a list of `{ column, type }`; the adapter matches a cached row by every key column, types a member action by its route's segments rather than `/:id`, and an optimistic store keeps the key columns the input carries. Done: OpenAPI, review emitter and client map tests on the kitchen-sink `order_items`, the client map golden and the fixtures regenerated, the shop fixture's `order_items` blend with seed rows and `orders` including `items`, adapter runtime tests (show by two params, an optimistic update touching only the row every column names, a store keeping the input's key, a destroy) and type tests, and D30's `key` sentence noting D33.
- [x] P16.16d Conformance and the docs (D33): the shop fixture's `order_items` (its table came with P16.16a, its blend, seed rows and `orders` including `items` with P16.16c) gets its cases, `COMPOSITE-KEY` (show, update, store with both key columns, a 409 on the key, the index order, destroy, and 404 for a pair that names no row or a segment of the wrong type) and `INCLUDE-COMPOSITE` (`orders/1?include=items`); the conformance spec describes the fixture; the guide (schema, blends, the HTTP API, the React client), cookbook pattern 16 with its compiled twin, the CLAUDE.md template and the roadmap. Done: the cases pass on PGlite, on PostgreSQL with Bun and on Node with pg, and the site build resolves every link and anchor.
- [x] P16.17 PUT, decided first (D34): `a.replace()` on `PUT /<table>/:id`, the store rules without the key columns, a missing writable column reset to its default or null, hidden columns left alone, 404 for a missing row, update's pipeline. Split into P16.17a and P16.17b.
- [x] P16.17a The replace action (D34): `a.replace()` as a built-in member action on `put`, `HttpMethod` gaining `'put'`; DR-REPLACE-BODY (the store rules minus the key columns, hidden columns optional) and DR-REPLACE-RESET (a writable, visible, non-key column the body leaves out goes to its default or to null) in the spec, with their tests and cases on the shop fixture (orders and order_items gain `a.replace()`); `defaultPrev` puts the nulls in `prev`, save writes SQL `DEFAULT` for the defaulted columns not in the writes, `resetColumns` names both sets; the hono adapter routes `put`. Done: `engine-replace.test.ts` on PGlite (a reset to null and to a default, a hidden column untouched unless sent, a required column missing is 422, a key column in the body is 422, calculate sees the nulls and keeps what it sets, 404, `updated_at` touched, a composite key), `replace.types.test.ts` and the hc `$put` route test, a hono run test, the spec entries and the cases on every runtime. Found on the way: a hidden column store requires would have made PUT impossible without sending it, so the rules leave hidden columns optional; D34 says so.
- [x] P16.17b PUT in OpenAPI, the review, the React adapter and the docs (D34): the `put` operation with its body; the review's stage wording for replace and a `resets` block naming the columns it sends back to their default and to null, one line each; `optimistic: true` on replace merges the body as update does; the guide (blends, the HTTP API, hooks, the React client), cookbook pattern 17 with its compiled twin, the CLAUDE.md template, the pipeline and conformance specs and the roadmap. Done: OpenAPI and review tests, an adapter runtime test and type test, and the site build resolving every link and anchor.
- [ ] P16.18 Publishing to npm, decided first (D35): what ships (`blendx`, `@blendx/cli`, `@blendx/react` and the packages the facade depends on), from what build output (TypeScript source with Node's type stripping, or compiled JavaScript with declarations), one version across the packages or several, the peer dependencies, provenance from CI, and a release process. Then the build items: package metadata and exports, a packing test that installs the tarballs into a fresh app and runs it on Node, the CI job, and the docs (getting started from npm).
