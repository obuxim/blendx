# Generated by `blendx review`. Do not edit: a change here is a fix request.
# Change the blend until `blendx review --check` passes.

format: 1
resource: order_notes
source: blends/order_notes.ts
record:
  id: integer
  order_id: integer
  author_id: integer, or null
  body: string
  created_at: string
includes:
  author: "users, through its show: authenticated"
actions:
  index:
    route: GET /order_notes
    input:
      page: string, matching ^[1-9][0-9]*$, optional
      per_page: string, matching ^[1-9][0-9]*$, optional
      sort: one of id, order_id, author_id, -id, -order_id, -author_id, optional
      id: string, optional
      order_id: string, optional
      author_id: string, optional
      include: string, optional
    stages:
      rules: page, per_page, sort, and filters on key and indexed columns
      load: a filtered, sorted page of rows
      authorize: authenticated
      calculate: nothing
      save: nothing
      respond: 200 with a page of records
    reply:
      status: 200
      body: a page of records

  store:
    route: POST /order_notes
    input:
      order_id: integer
      author_id: integer, or null, optional
      body: string
    stages:
      rules: the insert columns, without generated ones
      load: nothing
      authorize: authenticated
      calculate: the input's writable columns
      save: insert, setting created_at
      respond: 201 with the saved record
    calculate:
      writes: [author_id, body, order_id]
    reply:
      status: 201
      body: the record

  show:
    route: GET /order_notes/:id
    input:
      include: string, optional
    stages:
      rules: nothing (an empty object)
      load: the row by id
      authorize: authenticated
      calculate: nothing
      save: nothing
      respond: 200 with the record
    reply:
      status: 200
      body: the record
