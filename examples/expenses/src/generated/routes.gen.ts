// Generated by blendx from blends/. Do not edit; run `blendx generate`.

import { router, run } from "blendx";
import expenses from "../../blends/expenses.ts";
import users from "../../blends/users.ts";

export const routes = router()
  .get("/expenses", ...run(expenses, "index"))
  .post("/expenses", ...run(expenses, "store"))
  .get("/expenses/quote", ...run(expenses, "quote"))
  .get("/expenses/:id", ...run(expenses, "show"))
  .patch("/expenses/:id", ...run(expenses, "update"))
  .delete("/expenses/:id", ...run(expenses, "destroy"))
  .post("/expenses/:id/restore", ...run(expenses, "restore"))
  .post("/expenses/:id/approve", ...run(expenses, "approve"))
  .post("/expenses/:id/reject", ...run(expenses, "reject"))
  .post("/expenses/:id/submit", ...run(expenses, "submit"))
  .post("/users", ...run(users, "store"))
  .get("/users/:id", ...run(users, "show"));

export const resources = [expenses, users];

export type AppType = typeof routes;
