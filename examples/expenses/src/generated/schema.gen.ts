// Generated by blendx from schema.dbml. Do not edit; run `blendx generate`.

import { boolean, date, foreignKey, index, integer, numeric, pgEnum, pgTable, timestamp, uuid, varchar } from "blendx/drizzle";

export const expense_category = pgEnum("expense_category", ["travel", "meals", "office", "other"]);

export const expense_status = pgEnum("expense_status", ["draft", "submitted", "approved", "rejected"]);

export const users = pgTable("users", {
  id: integer().primaryKey().generatedAlwaysAsIdentity(),
  email: varchar({ length: 255 }).notNull().unique("users_email_key"),
  name: varchar({ length: 80 }).notNull(),
  is_approver: boolean().notNull().default(false),
  api_token: uuid().notNull().unique("users_api_token_key").defaultRandom(),
  created_at: timestamp({ mode: "string" }).notNull().defaultNow(),
  updated_at: timestamp({ mode: "string" }).notNull().defaultNow(),
});

export const expenses = pgTable("expenses", {
  id: integer().primaryKey().generatedAlwaysAsIdentity(),
  user_id: integer().notNull(),
  description: varchar({ length: 200 }).notNull(),
  category: expense_category().notNull(),
  amount: numeric({ precision: 10, scale: 2 }).notNull(),
  tax: numeric({ precision: 10, scale: 2 }).notNull(),
  total: numeric({ precision: 10, scale: 2 }).notNull(),
  status: expense_status().notNull().default("draft"),
  spent_on: date({ mode: "string" }).notNull(),
  review_note: varchar({ length: 500 }),
  created_at: timestamp({ mode: "string" }).notNull().defaultNow(),
  updated_at: timestamp({ mode: "string" }).notNull().defaultNow(),
  deleted_at: timestamp({ mode: "string" }),
}, (t) => [
  foreignKey({ name: "expenses_user_id_fkey", columns: [t.user_id], foreignColumns: [users.id] }),
  index("expenses_status_idx").on(t.status),
  index("expenses_spent_on_idx").on(t.spent_on),
]);

export const models = {
  users: {
    name: "users",
    table: users,
    meta: {
      primaryKey: "id",
      timestamps: { createdAt: "created_at", updatedAt: "updated_at" },
      softDelete: null,
      generated: ["id", "created_at", "updated_at"],
      constraints: {
        "users_pkey": { kind: "primaryKey", columns: ["id"] },
        "users_email_key": { kind: "unique", columns: ["email"] },
        "users_api_token_key": { kind: "unique", columns: ["api_token"] },
      },
    },
  },
  expenses: {
    name: "expenses",
    table: expenses,
    meta: {
      primaryKey: "id",
      timestamps: { createdAt: "created_at", updatedAt: "updated_at" },
      softDelete: "deleted_at",
      generated: ["id", "created_at", "updated_at", "deleted_at"],
      constraints: {
        "expenses_pkey": { kind: "primaryKey", columns: ["id"] },
        "expenses_user_id_fkey": { kind: "foreignKey", columns: ["user_id"], references: { table: "users", columns: ["id"] } },
      },
    },
  },
} as const;
